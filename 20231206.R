# a block of program
# code block

# example of a code block,
# programming block
{
  # code lines
}
 
# use it repetitively
# add for(...) in front of it
for(.x in ...)
{
  # code lines
}

# use the block somewhere else
# add function(...)
# and bind it with a name
myfun <- function(...)
{
  
}